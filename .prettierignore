# Claude Design 아트보드와 캔버스 매니페스트.
# 도구가 생성하고 수정하는 파일이므로 포맷하지 않는다.
wireframes/*.dc.html
wireframes/canvas.json

# 빌드 산출물과 잠금 파일
dist
pnpm-lock.yaml

# 마크다운은 포맷하지 않는다.
# Prettier가 표의 열 너비를 다시 계산하면서 한국어 문서의 정렬이 어긋난다.
# 가운뎃점(U+00B7)의 East Asian Width가 Ambiguous여서 Prettier는 1칸으로,
# 고정폭 한글 글꼴은 2칸으로 처리한다.
*.md

# 백엔드 저장소에서 가져온 Vercel Function.
# 계약 파일이라 형식을 바꾸지 않는다. 갱신할 때 원본과 그대로 대조해야 한다.
# 원본: be/deploy/public-gateway/gateway.mjs
#
# 한 곳만 원본과 다르다. 응답이 502가 되는 지점에 서버 로그를 남긴다.
# 다시 복사할 때 이 부분을 되돌리지 않도록 확인한다.
api/gateway.mjs
